import { Link } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            <Link to="/vehicles"> <button>Test</button> </Link>
        </div>
    );
};

export default Home;